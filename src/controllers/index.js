const {HomeController} = require('./HomeController');
const {SearchResultController} = require(`./SearchResultController`);
const {TrendingByEmailController} = require(`./TrendingByEmailController`);
const {TrendingsController} = require(`./TrendingsController`);
const {AboutMeController} = require(`./AboutMeController`);

module.exports = {
  HomeController,
  SearchResultController,
  TrendingByEmailController,
  TrendingsController,
  AboutMeController,
};
